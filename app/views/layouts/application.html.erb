<!DOCTYPE html>
<html>
  <head>
    <title>Topics</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="centrifugo-host" content="<%= ENV['CENTRIFUGO_HOST'] %>">
    <meta name="webpush-pubkey" content="<%= Webpush.public_key %>">

    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <link rel="manifest" href="/site.webmanifest">
  </head>

  <body>
    <%= yield %>
  </body>
</html>
